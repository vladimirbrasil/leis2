<%= form_for(@verbo) do |f| %>
  <% if @verbo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@verbo.errors.count, "error") %> prohibited this verbo from being saved:</h2>

      <ul>
      <% @verbo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
